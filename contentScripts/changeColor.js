/*
chrome.storage.sync.get("color", function (values){
    values = values.color;

    if (values === "yellow"){
        values = "pink";
    } else {
        values = "yellow";
    }
    chrome.storage.sync.set({color: values}, function(){
        console.log("Successfully set the highlight color to " + values);
    })
}) */


